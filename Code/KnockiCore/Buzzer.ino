/*
 * Fichier contenant toutes les fonctions utiles pour
 * effectuer des retours sonores à l'utilisateur avec
 * le buzzer actif
 */

/*
void buzzerSetup() {
  pinMode(BUZZER, OUTPUT);
  Serial.println("Buzzer successfully initialized");
}

void errorBuzz() {
  int toneLength = 200;
  tone(BUZZER, 300, toneLength);
  delay(400);
  tone(BUZZER, 300, toneLength);
}

void successBuzz() {
  int toneLength = 100;
  tone(BUZZER, 1000, toneLength);
  delay(100);
  tone(BUZZER, 1100, toneLength);
  delay(300);
  tone(BUZZER, 1300, toneLength);
}
*/
